# Basic Usage

```bash
orc init
orc ask "explain this project"
orc context "authentication"
orc edit "add request validation to the login route"
orc commit
```

`orc edit` always shows a diff and asks for approval before writing files.
