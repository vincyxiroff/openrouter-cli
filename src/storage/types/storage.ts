export type AppDataPaths = {
  root: string;
  trusted: string;
  globalConfig: string;
  modelsCache: string;
  plugins: string;
  pluginRegistryCache: string;
  updateCheck: string;
  logs: string;
  cache: string;
  authMetadata: string;
  apiKey: string;
  telemetry: string;
};

export type ProjectDataPaths = {
  root: string;
  history: string;
  projectConfig: string;
  filesCache: string;
  slashRecent: string;
  fileMentionsRecent: string;
  mcp: string;
  plugins: string;
  pluginsDir: string;
};
